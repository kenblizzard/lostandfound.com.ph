angular.module('UserProfileController', []).controller('UserProfileController', function ($scope) {

    $scope.tagline = 'To the moon and back!';

    $scope.searchItem = function () {
        alert('hi');
    }
});